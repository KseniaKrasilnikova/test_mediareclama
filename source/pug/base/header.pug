mixin header(headerMods, currentPage)
  -
    const headerLinks = [
      {
        href: "#",
        text: "Страница 1",
      },
      {
        href: "#",
        text: "Страница 2"
      },
      {
        href: "#",
        text: "Страница 3"
      },
      {
        href: "#",
        text: ""
      },
      {
        href: "#",
        text: "Страница 4"
      },
      {
        href: "#",
        text: "Страница 5"
      },
      {
        href: "#",
        text: "Страница 6"
      },
    ]


  header.header(class=headerMods)
    .container
      .header__content-wrapper
        button.header__menu-btn.button.button--transparent.btn-open-menu(type="button" aria-label="Открыть меню" aria-pressed="false")
          +icon("icon-burger", 28, 22)
        button.header__menu-btn.button.button--transparent.btn-close-menu(type="button" aria-label="Закрыть меню" aria-pressed="false")
          +icon("icon-cross", 14, 14)
        .header__top
          +search({mods: "custom-input--underlined", inputId: "search", inputName: "search", placeholder: "Поиск"})
          +icons-list()

        if(currentPage === "main.html")
          .header__logo
            .header__logo-img
              +img("content", "logo", "png", 160, 50, "Логотип")
        else
          a.header__logo(href="main.html", aria-label="Переход на главную")
            +img("content", "logo", "png", 160, 50, "Логотип")


        nav.main-nav
          ul.main-nav__list
            each link in headerLinks
              - const activeState = currentUrl === link.href
              li.main-nav__item
                if(activeState)
                  span.main-nav__link.is-active=link.text
                else
                  a.main-nav__link.hover-black(href=link.href)=link.text
        .bg-overlay

    .mobile-nav
      .mobile-nav__wrapper
        .container
          .mobile-nav__block
            ul.mobile-nav__list
              each link in headerLinks
                - const activeState = currentUrl === link.href
                li.mobile-nav__item
                  if(activeState)
                    span.mobile-nav__link.is-active=link.text
                  else
                    a.mobile-nav__link(href=link.href)=link.text
